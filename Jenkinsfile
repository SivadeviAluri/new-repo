pipeline {
    agent any {  
    }
    // environmental block
    environment {
        //key = value > ${key}
        name = "stygjiva"
        course = "devops engineer program"
    }
    stages {
        stage ('firstStage'){
            steps {
                echo "welcomhhe ${name}"
                echo "you enrolled to ${course}"

            }
        }
    }
}
