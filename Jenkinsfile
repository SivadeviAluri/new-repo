pipeline {
    agent any {  
    }
    // environmental block
    environment {
        //key = value > ${key}
        name = "siva"
        course = "devops engineer program"
    }
    stages {
        stage ('firstStage'){
            steps {
                echo "welcome ${name}"
                echo "you enrolled to ${course}"

            }
        }
    }
}
